## 1. Refuse 診斷

- [x] 1.1 新增 `matched_cannot_answer_marker`（或同等）＋單元測試（empty／命中「無法」／未命中）
- [x] 1.2 `RagAnswerService` MODEL_REFUSE 路徑 log `matched_marker` + `answer_preview`（≤200）＋測試
- [x] 1.3 `WebSearchService` 同路徑對稱 log＋測試
- [x] 1.4 勾選本 tasks；跑相關 pytest
