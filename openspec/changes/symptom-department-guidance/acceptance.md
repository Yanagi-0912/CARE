# 驗收標準：撤回人工補列與人工排序、修正卡片標註

- 建立日期：2026-09-11（同日追加：載入檢查、刪除程式中的 `kind`、保底加入不分科、H 節檢核清單）
- 對應：design 決策 6、14、15；spec「對照表載入時強制轉為部定專科並驗證來源」「建議為多候選並具保底」「候選排序只依來源資料」「候選標註與參考來源」；tasks 第 9 節
- **判定方式：H 節每一列都對應 `tests/unit/services/medical/test_symptom_acceptance.py` 裡的一個測試，全部通過才算驗收通過。K 節是不能寫成 pytest 的檢查，每項都要附上執行輸出。**

## 測試紀律

- 期望值從 spec 與原始對照表推導，在實作之前寫定。實作期間 SHALL NOT 為了讓測試通過而修改期望值；認為期望值有誤時，先回報，由人決定。
- 實作前先在舊程式上跑一次。H 節標「紅」的項目必須失敗，而且失敗原因要與「實作前為何失敗」一欄一致；標「綠」的是守既有行為的迴歸項，實作前後都必須通過。
- 不使用 `skip`、`xfail`，也不把 `==` 放寬成 `in` 之類的寬鬆斷言。
- 斷言使用者看得到的輸出（卡片文字、候選順序、參考來源），不只測內部函式。
- 載入測試以 `tmp_path` 寫出對照表，透過 `load_symptom_table(path)` 注入，不用 monkeypatch。

## 範圍

**包含**

- 刪除對照表中 16 筆 `origin: project` 候選與 11 個 `rank` 欄位
- 載入檢查：欄位白名單、`term` 不可為空、`sources` 不可為空或重複或含未登記代碼、「症狀＋科別」全表唯一、`db_facility_count` 為正整數
- 刪除程式中的 `SymptomEntry.kind`；JSON 保留 `kind` 供維護者分類
- 排序改為「來源家數 → 院所數」
- 卡片標註改為「N 家中 M 家」，`note` 不上卡片，刪除「列在其他科別」分支
- 保底科別改為家醫科、內科、不分科

**不包含**（diff 中出現即不通過）

- 用語改寫（31 筆）與次專科標籤：維持現狀
- `MAX_CANDIDATES` 的數值、兒科過濾規則、急迫度判斷、正規化與向量門檻
- 新增任何來源醫院的資料（屬 G 節，下一階段）

## A. 對照表結構

- A1 每個候選至少 1 個來源代碼，代碼不重複且都登記在表頭 `sources` 區塊（H：T01、T02、T05、T10）
- A2 症狀條目只含白名單欄位 `term`、`sources`、`subgroup`、`kind`、`note`、`downgraded_from`（H：T00、T03、T10）
- A3 `term` 不可為空；「症狀＋科別」全表唯一；`db_facility_count` 為正整數（H：T04、T06、T07）
- A4 表中不再有 `流鼻水`、`流鼻血`、`痰多`、`帶狀皰疹（皮蛇）`（H：T10）
- A5 表頭 `version` 升為 4；`usage_rules` 不再描述 `origin`、`rank`，改寫為「只收來源所載、欄位白名單」，且不寫死醫院家數（K1）
- A6 向量檔已重建，啟動日誌沒有「向量檔與對照表不同步」（K5）

## B. 排序

- B1 全表每個症狀的候選依（來源家數由多到少，院所數由多到少）排列（H：T11、T12）
- B2 `DepartmentCandidate` 只有 `canonical`、`subgroup`、`facility_count`、`sources`；`SymptomEntry` 只有 `term`、`candidates`（H：T09）

## C. 卡片內容

- C1 全表每個條目以 40 歲與 8 歲各產生一張卡，都不含：`註：`、`補列`、`人工改寫`、`原註記`、`SHALL`、`downgrade_rule`、`cos(`、`爬蟲`、`三家`、`都這樣分類`（H：T25）
- C2 每個候選的理由最後都有且只有一句標註，句型如下（H：T20–T25、T28）
- C3 參考來源區塊的標題固定為「參考來源」，只列支持至少一個已顯示候選的醫院，順序依表頭 `sources`（H：T28）
- C4 保底卡沒有標註，也沒有參考來源區塊（H：T26）
- C5 最大尺寸的卡片（5 個候選、每個兩個次專科標籤、最長標註）通過 LINE SDK 驗證（H：T27）

C2 的標註句型（N＝收錄此症狀的醫院數，同一家列在多科只算一次；M＝列在此科的醫院數）：

| 條件 | 標註 |
|---|---|
| N = 1 | （僅 1 家醫院的對照表收錄此症狀，建議先去電確認） |
| M = 1 且 N > 1 | （收錄此症狀的 N 家醫院中，有 1 家列在此科，建議先去電確認） |
| 其餘 | （收錄此症狀的 N 家醫院中，有 M 家列在此科） |

## D. 行為案例

以真實對照表執行，正規化器以替身直接回傳條目。來源代碼：V＝臺北榮總玉里分院、N＝成大醫院、Y＝台大雲林分院。「3 家中 2 家」為簡寫，實際文字依 C2 句型；「＋去電」表示句尾附「建議先去電確認」。預期值由原始 JSON 直接推導，不經過服務程式。

| # | 條目 | 年齡 | 預期結果 | 候選（依序）與標註 | 參考來源 |
|---|---|---|---|---|---|
| D1 | 咳嗽 | 40 | 建議 | 內科〔胸腔內科〕3 家中 2 家 | N、Y |
| D2 | 咳嗽 | 8 | 建議 | 內科〔胸腔內科〕3 家中 2 家；兒科 3 家中 1 家＋去電 | V、N、Y |
| D3 | 嘔吐 | 40 | 保底：「這個症狀在對照表中只列了兒科」，給家醫科、內科、不分科 | — | 無 |
| D4 | 嘔吐 | 8 | 建議 | 兒科：僅 1 家收錄＋去電 | V |
| D5 | 慢性咳嗽 | 40 | 保底（同 D3） | — | 無 |
| D6 | 坐骨神經痛 | 40 | 建議 | 神經外科 3 家中 2 家；復健科 3 家中 1 家＋去電；骨科 3 家中 1 家＋去電 | V、N、Y |
| D7 | 性病 | 40 | 建議 | 內科〔感染科〕2 家中 2 家；皮膚科 2 家中 1 家＋去電；泌尿科 2 家中 1 家＋去電 | N、Y |
| D8 | 感冒 | 40 | 建議 | 內科 2 家中 2 家 | N、Y |
| D9 | 氣喘 | 40 | 建議 | 內科〔胸腔內科〕3 家中 2 家 | N、Y |
| D10 | 高血脂 | 40 | 建議 | 內科〔新陳代謝內分泌科／心臟內科〕2 家中 2 家 | N、Y |
| D11 | 酒癮 | 40 | 建議 | 精神科 3 家中 3 家 | V、N、Y |
| D12 | 身心障礙者牙科照護 | 40 | 建議 | 牙科〔特殊需求者牙科〕僅 1 家收錄＋去電 | Y |
| D13 | 頭痛 | 40 | 建議 | 神經科 3 家中 3 家；家醫科 3 家中 1 家＋去電 | V、N、Y |
| D14 | 尿床 | 40 | 保底（同 D3） | — | 無 |
| D15 | 流鼻水、流鼻血、痰多、皮蛇（口語原句，走真實正規化器） | 40 | 預期保底；若對應到其他條目，記錄實際條目交人工判斷；對應到疾病名（鼻炎、鼻過敏）即不通過 | — | — |
| D16 | 腹瀉 | 40 | 建議（榮總玉里列在內科與兒科，分母只算一次） | 內科〔胃腸肝膽科〕2 家中 2 家 | V、N |

D3、D5 由 T15 驗證，其餘建議案例與 D14 由 T28 驗證，D15 為 K4。

## E. 程式碼清理

- E1 `app/services/medical/symptom_classification/` 與 `symptom_department_flex_message.py` 中以 `\brank\b|_UNRANKED|\borigin\b|\.note\b|note=|kinds\b` 搜尋，結果為 0 筆（K1）
- E2 `app/`、`resources/`、`tests/` 中搜尋 `_cites_other_department`、`_SOURCE_LABEL_OTHER_DEPARTMENT`、`列在其他科別`，結果為 0 筆（K1）
- E3 模組註解與 docstring 中描述 rank、補列、「上限 3」「至多 3 個候選」的段落已改寫或刪除（K1）
- E4 `app/`、`resources/`、`scripts/` 底下沒有新增檔案；沒有新增設定旗標或環境變數（K1）
- E5 白名單常數逐欄寫上意義註解，與 spec 欄位表一致（K1）

## F. 回歸

- F1 `venv/Scripts/python -m pytest tests/unit/services/medical tests/unit/tools/test_symptom_tools.py` 全綠
- F2 `venv/Scripts/python -m pytest tests/unit` 唯一的失敗仍是本機既有的 magick 測試（實作前基準：3993 passed、1 failed）

## G. 下一階段：併入更多醫院時的驗收（本次不執行）

每次併入新醫院資料，都 SHALL 通過下列檢查才能上線：

| # | 標準 |
|---|---|
| G1 | 原文備份存成 `raw/<醫院>_source.md`，格式比照 `vghtpe_yuli_source.md`（來源網址、網站更新日期、取得日期、網頁內容表格） |
| G2 | 表頭 `sources` 登記新代碼、名稱、網址、取得日期與層級 |
| G3 | 每一筆新增的「症狀－科別－來源」都能在 raw 原文中找到對應列；已存在的「症狀＋科別」只把代碼加進既有列的 `sources`，不新增一列 |
| G4 | 新增的 `term` 依 design 決策 11 改寫，不逐字搬原文長句 |
| G5 | 重跑 `scripts/build_symptom_vectors.py` |
| G6 | 產出差異報告交人工審閱：(a) 首選科別改變的症狀；(b) 在建議與保底之間互換的症狀，並標明是否因候選數超過 `MAX_CANDIDATES`；(c) 本次撤回、之後重新有來源的症狀（嘔吐、流鼻水等）；(d) 新增的條目；(e) 候選數大於 3 的症狀清單，作為 design 決策 7 上限複議的依據 |
| G7 | H 節全數重跑通過 |

## H. 測試檢核清單

全部位於 `tests/unit/services/medical/test_symptom_acceptance.py`，測試名稱以編號開頭。「實作前」一欄是 2026-09-11 在舊程式上應有的結果。

| ID | 驗什麼 | 預期 | 實作前 | 實作前為何失敗 |
|---|---|---|---|---|
| T00 | 白名單欄位全部出現的最小合法表 | 可載入 | 綠 | — |
| T01 | `sources` 為空 | 拋錯，訊息含「sources 為空」 | 紅 | 舊載入器接受空來源 |
| T02 | `sources` 含未登記代碼 | 拋錯，訊息含「未登記的來源代碼」 | 紅 | 不檢查代碼 |
| T03 | 帶 `rank`、`origin`、`priority` | 拋錯，訊息含「不允許的欄位」 | 紅 | 不檢查欄位 |
| T04 | 同區塊重複、兩個同科區塊重複 | 拋錯，訊息含「重複的症狀與科別」 | 紅 | 不檢查重複 |
| T05 | `sources` 內代碼重複 | 拋錯，訊息含「來源代碼重複」 | 紅 | 不檢查 |
| T06 | `term` 為空字串或只有空白 | 拋錯，訊息含「term 為空」 | 紅 | 空 term 被默默略過 |
| T07 | `db_facility_count` 缺漏、null、0、負數、字串、小數、布林 | 拋錯，訊息含「db_facility_count」 | 紅 | 缺值轉成 0，其餘被 `int()` 轉換 |
| T08 | `status` 為 unverified | 可載入，日誌含「非 verified」 | 綠 | — |
| T09 | 資料結構欄位 | 如 B2 | 紅 | 仍有 `rank`、`note`、`kind` |
| T10 | 正式表 | 欄位全在白名單、每個候選有來源、4 個撤回條目不存在 | 紅 | 仍有 `rank`、`origin` 與補列條目 |
| T11 | 全表排序 | 如 B1 | 紅 | 咳嗽、坐骨神經痛等由 rank 決定順序 |
| T12 | 坐骨神經痛、性病的順序 | 神經外科→復健科→骨科；內科→皮膚科→泌尿科 | 紅 | 仍依 rank |
| T13 | 咳嗽、感冒、氣喘、高血脂（40 歲） | 只剩內科 | 紅 | 仍有補列的家醫科、耳鼻喉科 |
| T14 | 保底科別 | 家醫科、內科、不分科（字面值），都是資料庫科別 | 紅 | 只有家醫科、內科 |
| T15 | 嘔吐、慢性咳嗽（40 歲） | 保底，原因「這個症狀在對照表中只列了兒科」，給 T14 的三科 | 紅 | 仍有補列的內科 |
| T16 | 嘔吐（8 歲） | 只給兒科 | 紅 | 還看得到補列的內科 |
| T17 | 尿床 14 歲、15 歲 | 14 歲給兒科，15 歲走保底 | 綠 | — |
| T18 | 年齡未知、-1、131、"12"、`True`、12.5；年齡未知但訊息提到「我兒子」 | 前者一律當成人（尿床走保底），後者給兒科 | 綠 | — |
| T19 | 候選數 5、6、6 含兒科（成人） | 建議 5 個、保底、保底 | 綠 | — |
| T20 | 標註 N = 1 | 「（僅 1 家醫院的對照表收錄此症狀，建議先去電確認）」 | 紅 | 舊句型「僅一家…這樣分類」 |
| T21 | 標註 M = 1 < N | 「（收錄此症狀的 3 家醫院中，有 1 家列在此科，建議先去電確認）」 | 紅 | 同上 |
| T22 | 標註 M = N > 1 | 「（收錄此症狀的 3 家醫院中，有 3 家列在此科）」，不含「都」 | 紅 | 舊句型「三家…都這樣分類」 |
| T23 | 標註 M < N | 「（收錄此症狀的 3 家醫院中，有 2 家列在此科）」 | 紅 | 2 家時完全不加註 |
| T24 | 5 家來源、其中 3 家列在此科 | 「（收錄此症狀的 5 家醫院中，有 3 家列在此科）」，卡片不含「三家」 | 紅 | 寫死「三家」 |
| T25a | 全表 × {40 歲, 8 歲} 的卡片文字 | 如 C1 | 紅 | note 印在卡片上（「註：」等），標註寫死「三家」 |
| T25b | 全表 × {40 歲, 8 歲} 的每個候選 | 理由結尾剛好一句標註 | 紅 | 2 家來源時完全不加註 |
| T26 | 保底卡 | 如 C4 | 綠 | — |
| T27 | 最大尺寸卡片 × 三種字級 | 如 C5 | 綠 | — |
| T28 | D 節 D1、D2、D4、D6–D14、D16 | 候選、次專科、順序、標註、參考來源全部符合 D 節 | 紅 | 候選、標註或保底清單不同 |

## K. 不能寫成 pytest 的檢查（須附輸出）

| ID | 檢查 | 通過條件 |
|---|---|---|
| K1 | E1–E5 的 grep 與人工檢視；A5 | 全部符合 |
| K2 | 行為快照比對：以實作前的全表快照（`baseline_snapshot.json`，K2 通過後已刪除，不進 commit）對照實作後的全表 × {40 歲, 8 歲} | 建議卡的候選清單只有 D 節列出的 12 個症狀改變；保底卡只比「是否保底」與保底原因（候選清單因加入不分科必然改變） |
| K3 | 突變驗證：暫時還原 note 拼接、還原 rank 排序、拿掉空來源檢查、把標註寫回「三家」、把保底改回兩科 | 分別讓 T25a、T11、T01、T24、T14 轉紅；還原後恢復全綠 |
| K4 | D15 線上抽測（需真實向量與 LLM） | 如 D15 |
| K5 | 重建向量檔後啟動服務 | 日誌沒有「向量檔與對照表不同步」 |
